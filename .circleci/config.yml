version: 2.1
orbs:
  node: circleci/node@4.1.1
jobs:
  test:
    executor: node/default
    steps:
      - checkout
      - node/install-packages
      - run: npm run test
workflows:
  test:
    jobs:
      - test
